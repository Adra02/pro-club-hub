{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
```

---

## 📸 Screenshot Guida Vercel Settings

Quando vai su Settings → General → Build & Development Settings, dovresti vedere:
```
Framework Preset
[ Other                           ▼ ]

Root Directory
[                                   ]  (VUOTO)

Build Command
Override: [✓]
[                                   ]  (VUOTO o "echo 'No build'")

Output Directory  
Override: [✓]
[                                   ]  (VUOTO - CANCELLA "dist")

Install Command
[npm install                        ]
